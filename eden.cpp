#include <iostream>
#include <list>
using namespace std;
int main(){
    list<int> eden;

    for (int i = 0; i < s.size(); i++)
    {
        char c = s[i];
        if (c>15)
    }

    int shuma = 0;
    for (auto i = eden.begin(); i!=eden.end(); i++)
    {
        shuma += *i;
    }

    cout << "Shuma: " << shuma;

    return 0;

 
}